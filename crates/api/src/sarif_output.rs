//! Compatibility re-exports for SARIF assembly now owned by `fallow-output`.

pub use fallow_output::{
    annotate_sarif_results, build_duplication_sarif, build_grouped_duplication_sarif,
    build_health_sarif,
};
